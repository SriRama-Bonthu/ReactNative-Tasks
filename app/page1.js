import React, { useState } from "react";
import { View, Text, StyleSheet, TouchableOpacity,Button,Image } from "react-native";

export default function Page1() {
    <View style={{ flex: 1 }}>
        <Text onPress={() => {}}>Page 1</Text>
    </View>
}